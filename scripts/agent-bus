#!/usr/bin/env bash
# Wrapper for the agent collaboration bus (Cursor ↔ Claude VS Code).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
exec python3 -m applicationbot.agent_bus "$@"
